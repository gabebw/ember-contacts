<div class="contact-list">
  <h2>Your contacts</h2>
  {{link-to 'Add new contact' 'contacts.new' tagName='button'}}
  {{contact-list contacts=model}}
</div>

<div class="contact">
  {{outlet}}
</div>
